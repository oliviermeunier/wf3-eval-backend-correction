<article>
    <h3><?=htmlspecialchars(mb_strtoupper($product['title']));?></h3>
    <p><?=htmlspecialchars($product['description']);?></p>
    <p><?=number_format($product['price']/100, 2, ',');?> €</p>
    <p><?=htmlspecialchars($product['city']);?> <?=htmlspecialchars($product['postal_code']);?></p>
</article>

<section>
    <h3>Réservez le produit</h3>
    <?php if($product['reservation_text']): ?>
        <h4>Ce produit est réservé</h4>
        <h4>Message de réservation</h4>
        <p><?=htmlspecialchars($product['reservation_text']);?></p>
    <?php else: ?>
        <form action="product.php?id=<?=intval($id);?>" method="POST">
            <div>
                <label for="message">Message de réservation</label>
                <textarea name="message"></textarea>
            </div>
            <input type="submit" value="Réserver">
        </form>
    <?php endif; ?>
</section>